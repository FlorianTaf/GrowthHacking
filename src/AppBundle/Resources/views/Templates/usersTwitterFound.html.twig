<table id="tabResultTwitterUser" class="tableau">
    <thead>
    <tr>
        <th>UserId</th>
        <th>Nom</th>
        <th>Tweet</th>
        <th>Options</th>
    </tr>
    </thead>
    <tbody>
    {% if tabResult is not empty %}
        {% for user in tabResult %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.name }}</td>
                <td class="width20-text padding-10">{{ user.text }}</td>
                <td>
                    <a href="https://www.twitter.com/{{ user.name }}" target="_blank">
                        {% if user.id in contactedUsers %}
                            <div class="already-contacted-btn btn red-btn margin-10" data-id={{ user.id }} data-info='{{ user.text }}' data-name='{{ user.name }}'>Profil consulté</div>
                        {% else %}
                            <div class="contact-twitter-user-btn btn green-btn margin-10" data-id={{ user.id }} data-info='{{ user.text }}' data-name='{{ user.name }}'>Consulter Profil</div>
                        {% endif %}
                    </a>
                </td>
            </tr>
        {% endfor %}
    {% else %}
        <tr>
            <td colspan=3>Aucun utilisateur trouvé</td>
        </tr>
    {% endif %}
    </tbody>
</table>